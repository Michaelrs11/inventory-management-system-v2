@page
@model IMS.BE.Pages.Transaction.OutboundModel
@{
    ViewData["Title"] = "Outbound";
}
<div class="card">
    <div class="card-body">
        <form method="post">
            @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
            {
                <div class="text-danger">
                    @Model.ErrorMessage
                </div>
            }
            <div class="form-row">
                <div class="form-group col-sm-12">
                    <div class="form-row">
                        <div class="col-sm-6">
                            <label class="form-label" asp-for="OutboundData.GudangCode">Gudang</label>
                            <select class="form-select" asp-for="OutboundData.GudangCode" asp-items="Model.GudangCodeDropdown">
                                <option value="">--Select Gudang--</option>
                            </select>
                            <span asp-validation-for="OutboundData.GudangCode" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="col-sm-6">
                            <label class="form-label" asp-for="OutboundData.SkuCode">Barang</label>
                            <select class="form-select" asp-for="OutboundData.SkuCode" asp-items="Model.BarangCodeDropdown">
                                <option value="">--Select Barang--</option>
                            </select>
                            <span asp-validation-for="OutboundData.SkuCode" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="col-sm-6">
                            <label for="status">Stock Out</label>
                            <input type="number" class="form-control" asp-for="OutboundData.StockOut" />
                            <span asp-validation-for="OutboundData.StockOut" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group col-sm-12 mt-auto d-flex justify-content-end ml-auto">
                            <button type="submit" class="btn btn-primary mt-auto">Simpan</button>
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>